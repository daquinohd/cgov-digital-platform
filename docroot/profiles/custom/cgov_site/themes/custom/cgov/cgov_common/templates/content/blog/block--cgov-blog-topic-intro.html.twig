{% if content['#topic_intros']|length > 0 %}
  <div class="blog-intro-text">
    {% for title, link in content['#topic_intros'] %}
      <p>{{title}}, {{link}} ({{get.topic}})</p>
    {% endfor %}    
  </div>
{% endif %}
