{% if content['#topic_intros']|length > 0 %}
  <div class="blog-intro-text">
    {% for title, desc in content['#topic_intros'] %}
      {% if title == label %}
        <p>{{desc}}</p>
      {% endif %}
    {% endfor %}    
  </div>
{% endif %}
